using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum PieceType {
    progressor = 0,
    liberator = 1,
    intellector = 2,
    dominator = 3, 
    defensor = 4, 
    agressor = 5
}
